
import mapNodesToPath from './mapNodesToPath';

describe('mapNodesToPath', () => {
  it('should render successfully', () => {
    // const nodes = mapNodesToPath();
    // expect(baseElement).toBeTruthy();
  });
});
